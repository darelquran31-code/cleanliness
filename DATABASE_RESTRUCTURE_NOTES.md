# تعديلات بنية قاعدة البيانات - توحيد توزيع مواد النظافة

## 📋 الملخص
تم تعديل بنية قاعدة البيانات لتوحيد كميات مواد النظافة لجميع المساجد الـ 1700. بدلاً من استخدام جدول منفصل للتخصيصات، تم إضافة عمود `Quantity` مباشرة في جدول `Materials`.

---

## 🗂️ التعديلات على جداول Google Sheets

### جدول Materials (الجديد)
**الأعمدة:**
```
A: Name (اسم المادة)
B: Unit (الوحدة)
C: Quantity (الكمية الموحدة لكل مسجد)
```

**مثال:**
| Name | Unit | Quantity |
|------|------|----------|
| صابون ارضية سائل | لتر | 30 |
| مواد مطهرة | لتر | 20 |
| ... | ... | ... |

### جدول Allocations
❌ **تم حذفه تماماً** - لم نعد نحتاجه

---

## 💻 التعديلات على الـ Backend

### `/backend/routes/admin.js`

#### 1. تعديل Endpoint: `POST /add-material`
**المتطلبات القديمة:**
```javascript
{ name, unit }
```

**المتطلبات الجديدة:**
```javascript
{ name, unit, quantity }
```

**المثال:**
```javascript
{
  "name": "صابون ارضية سائل",
  "unit": "لتر",
  "quantity": 30
}
```

#### 2. تعديل Endpoint: `GET /materials`
الآن يعيد البيانات مع الكميات:
```javascript
[
  {
    id: 1,
    name: "صابون ارضية سائل",
    unit: "لتر",
    quantity: 30
  }
]
```

#### 3. تعديل Endpoint: `GET /allocations/:mosque`
لم يعد يأخذ بيانات من جدول منفصل. الآن يعيد جميع المواد مع الكميات الموحدة:
```javascript
// جميع المساجد تحصل على نفس البيانات
GET /allocations/المسجد_الأول
GET /allocations/أي_مسجد_آخر
// كلاهما يعيد نفس المواد والكميات
```

#### 4. حذف Endpoint: `POST /add-allocation`
❌ **تم حذفه** - لم نعد نحتاجه

---

## 🎨 التعديلات على الـ Frontend

### 1. `/frontend/src/services/api.js`
- ✅ تحديث `addMaterial()` ليقبل معامل `quantity`
- ❌ حذف `addAllocation()` - لم نعد نحتاجه

### 2. `/frontend/src/components/MaterialManagement.jsx`
- ✅ إضافة حقل Input للكمية
- ✅ تحديث عرض الجدول ليشمل عمود الكمية
- ✅ تحديث رسالة الخطأ لتشمل الكمية

**شكل الفورم الجديد:**
```
اسم المادة: [_____________]
الوحدة: [_____________]
الكمية لكل مسجد: [_____________]
```

### 3. `/frontend/src/pages/AdminDashboard.jsx`
- ✅ إزالة Tab "تخصيص الكميات"
- ✅ حذف import للـ AllocationManagement

### 4. `/frontend/src/components/AllocationManagement.jsx`
- ❌ **تم حذف الملف بالكامل** - لم نعد نحتاجه

### 5. `/frontend/src/components/ReceiptForm.jsx`
- ✅ **لا تعديلات مطلوبة** - يعمل تلقائياً مع البيانات الجديدة
- البيانات من `getAllocations()` ستأتي من جدول Materials مباشرة

---

## 📝 كيفية الاستخدام

### إضافة مادة نظافة جديدة:

1. اذهب إلى **لوحة الإدارة** → **مواد النظافة**
2. امل النموذج:
   - **اسم المادة**: مثل "صابون ارضية سائل"
   - **الوحدة**: مثل "لتر" أو "علبة"
   - **الكمية لكل مسجد**: مثل "30"
3. اضغط **إضافة مادة**

### التوزيع التلقائي:
- عند إضافة مسجد جديد عبر "المستخدمين"
- سيحصل على جميع المواد الموجودة في جدول Materials
- بالكميات الموحدة المحددة

---

## 🔄 الفوائد

✅ **توحيد الكميات**: جميع المساجد تحصل على نفس الكميات  
✅ **تبسيط قاعدة البيانات**: عمود واحد بدلاً من جدول كامل  
✅ **سهولة التحديث**: تعديل الكمية في مكان واحد لجميع المساجد  
✅ **أداء أفضل**: استعلامات أقل وبيانات منظمة أفضل  
✅ **واجهة مستخدم أوضح**: أقل خيارات، أسهل فهم  

---

## ⚠️ ملاحظات مهمة

### Google Sheets
اتأكد من:
1. جدول `Materials` يحتوي على 3 أعمدة فقط: Name, Unit, Quantity
2. جدول `Allocations` محذوف أو فارغ
3. البيانات القديمة محفوظة في ورقة backup إن أمكن

### استثناء محتمل
إذا كنت تريد كميات مختلفة لمساجد معينة لاحقاً:
- ستحتاج إلى إنشاء جدول `Allocations` جديد
- مع هيكل: `mosque | materialId | quantity`
- وتعديل الـ endpoints لدعم هذا

---

## 🧪 الاختبار

### بعد التعديلات، اختبر:
1. ✅ إضافة مادة جديدة مع كمية
2. ✅ عرض قائمة المواد بالكميات
3. ✅ فتح فاتورة استلام والتحقق من الكميات
4. ✅ إضافة مسجد جديد والتحقق من حصوله على الكميات

---

## 📞 للدعم
إذا واجهت أي مشاكل أو كان لديك سؤال، تفقد الملفات المعدلة:
- `backend/routes/admin.js`
- `frontend/src/services/api.js`
- `frontend/src/components/MaterialManagement.jsx`
- `frontend/src/pages/AdminDashboard.jsx`