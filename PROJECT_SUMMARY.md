# ملخص المشروع - نظام استلام مواد النظافة للمساجد

## 📋 نظرة عامة
تطبيق ويب متكامل (Full-Stack) لتسيير استلام مواد النظافة في المساجد باستخدام:
- **Backend**: Node.js + Express
- **Frontend**: React + Vite
- **Database**: Google Sheets

---

## 📁 هيكل المشروع

```
eslam/
├── backend/                    # سيرفر Node.js
│   ├── server.js              # الملف الرئيسي
│   ├── package.json           # الحزم المثبتة
│   ├── .env.example           # متغيرات البيئة
│   ├── credentials.json       # Google Service Account (ضعه هنا)
│   ├── services/
│   │   └── sheetsService.js   # خدمة Google Sheets
│   ├── routes/
│   │   ├── auth.js            # مسارات تسجيل الدخول
│   │   ├── admin.js           # مسارات الإدارة
│   │   └── user.js            # مسارات المستخدم
│   └── middleware/
│       └── auth.js            # تحقق من التوكن
│
├── frontend/                  # تطبيق React
│   ├── src/
│   │   ├── pages/
│   │   │   ├── Login.jsx      # صفحة تسجيل الدخول
│   │   │   ├── Dashboard.jsx  # لوحة المتابعة (مستخدم عادي)
│   │   │   └── AdminDashboard.jsx  # لوحة الإدارة
│   │   ├── components/
│   │   │   ├── ReceiptForm.jsx      # نموذج الاستلام
│   │   │   ├── UserManagement.jsx   # إدارة المستخدمين
│   │   │   ├── MaterialManagement.jsx    # إدارة المواد
│   │   │   └── AllocationManagement.jsx  # إدارة التخصيصات
│   │   ├── context/
│   │   │   └── AuthContext.jsx  # إدارة المصادقة
│   │   ├── services/
│   │   │   └── api.js          # استدعاءات API
│   │   ├── styles/             # ملفات CSS
│   │   ├── App.jsx             # المكون الرئيسي
│   │   └── main.jsx            # نقطة البداية
│   ├── index.html
│   ├── package.json
│   └── vite.config.js
│
├── README.md                   # دليل شامل
├── QUICKSTART.md               # دليل البدء السريع
├── GOOGLE_SHEETS_SETUP.md      # إعداد Google Sheets
└── PROJECT_SUMMARY.md          # هذا الملف

```

---

## 🎯 المميزات الرئيسية

### 1️⃣ نظام الدخول
- ✅ تسجيل دخول بالرقم المدني
- ✅ كلمة المرور الأولية = الرقم المدني
- ✅ تغيير كلمة المرور
- ✅ توكن JWT آمن

### 2️⃣ لوحة المتابعة (Dashboard)
للمستخدم العادي (الإمام/المؤذن):
- ✅ إضافة فاتورة استلام جديدة
- ✅ إدخال بيانات المسجد (اسم، منطقة، قطعة)
- ✅ إدخال بيانات العامل (الاسم، الرقم المدني)
- ✅ تسجيل الكميات المستلمة من كل مادة
- ✅ عرض الفواتير السابقة

### 3️⃣ لوحة الإدارة (Admin Dashboard)
للمسؤول الأول:

**إدارة المستخدمين**
- ✅ إضافة مستخدمين جدد
- ✅ عرض قائمة المستخدمين
- ✅ كل مستخدم مرتبط بمسجد معين

**إدارة مواد النظافة**
- ✅ إضافة مادة نظافة
- ✅ تحديد الوحدة (عبوة، لتر، إلخ)
- ✅ عرض قائمة المواد

**تخصيص الكميات**
- ✅ تحديد الكمية المفروض استلامها لكل مسجد
- ✅ ربط الكمية بمادة معينة
- ✅ إمكانية تخصيص كميات مختلفة حسب المسجد

---

## 🔧 البيانات المطلوبة

### 1. Google Sheets
يجب أن تحتوي الورقة على 5 tabs:

| Tab | الأعمدة |
|-----|--------|
| **Users** | nationalId, name, mosque, passwordHash |
| **Materials** | name, unit |
| **Allocations** | mosque, materialId, quantity |
| **Receipts** | timestamp, userNationalId, userName, mosque, zone, section, workerName, workerNationalId |
| **ReceiptDetails** | timestamp, materialId, allocatedQuantity, receivedQuantity |

### 2. المواد (19 مادة نظافة)
1. منظف الأرضيات
2. صابون سائل
3. مطهرات
4. فرش التنظيف
5. ممسحة الأرضيات
6. دلو
7. منظف الزجاج
8. ورق التنظيف
9. قفازات
10. فرشاة الحمام
11. مزيل الرائحة
12. معقم اليدين
13. مناديل ورقية
14. كيس نفايات
15. ممسحة الغبار
16. منظف معادن
17. محقنة التنظيف
18. إسفنجة
19. صابون صلب

---

## 🚀 خطوات التشغيل

### المتطلبات
```
✅ Node.js 14+ (مثبت بالفعل)
✅ npm (مثبت مع Node.js)
✅ حساب Google Cloud
```

### 1. إعداد Backend
```powershell
cd backend

# تثبيت الحزم
npm install

# إنشاء ملف .env
# أضف:
# GOOGLE_SHEETS_ID=معرف_ورقتك
# GOOGLE_SERVICE_ACCOUNT_JSON=./credentials.json
# JWT_SECRET=مفتاح_سري
# PORT=5000
```

### 2. إعداد Frontend
```powershell
cd frontend
npm install
```

### 3. إنشاء Google Sheet
- اذهب إلى https://sheets.google.com
- أنشئ ورقة جديدة
- شاركها مع Service Account
- أضف 5 tabs كما موضح أعلاه

### 4. التشغيل
```powershell
# Terminal 1 - Backend
cd backend
npm start

# Terminal 2 - Frontend
cd frontend
npm run dev
```

---

## 📡 مسارات API

### المصادقة
```
POST   /api/auth/login                 # تسجيل الدخول
POST   /api/auth/change-password       # تغيير كلمة المرور
```

### الإدارة
```
POST   /api/admin/add-user             # إضافة مستخدم
GET    /api/admin/users                # الحصول على المستخدمين
POST   /api/admin/add-material         # إضافة مادة
GET    /api/admin/materials            # الحصول على المواد
POST   /api/admin/add-allocation       # إضافة تخصيص
GET    /api/admin/allocations/:mosque  # الحصول على التخصيصات
```

### المستخدم
```
POST   /api/user/add-receipt           # إضافة فاتورة
GET    /api/user/receipts              # الحصول على الفواتير
```

---

## 🔐 الأمان

- ✅ توكن JWT للمصادقة
- ✅ تشفير bcryptjs لكلمات المرور
- ✅ تحقق من صلاحيات المسؤول
- ✅ معالجة الأخطاء الآمنة

---

## 📱 واجهة المستخدم

### شاشة تسجيل الدخول
- ✅ إدخال الرقم المدني
- ✅ إدخال كلمة المرور
- ✅ عرض الأخطاء بوضوح

### لوحة المتابعة
- ✅ عرض بيانات المستخدم
- ✅ نموذج استلام بسيط وسهل
- ✅ جدول بالمواد والكميات

### لوحة الإدارة
- ✅ 3 أقسام (المستخدمين، المواد، التخصيصات)
- ✅ نماذج إضافة بسيطة
- ✅ جداول عرض البيانات

---

## 🎨 التصميم

- ✅ واجهة حديثة وجميلة
- ✅ ألوان احترافية (أزرق وأرجواني)
- ✅ سريعة الاستجابة (Responsive)
- ✅ دعم اللغة العربية
- ✅ تصميم يتناسب مع الهاتف

---

## 📚 الملفات التوضيحية

| الملف | الوصف |
|-----|--------|
| **README.md** | دليل شامل عن المشروع |
| **QUICKSTART.md** | دليل البدء السريع (5 دقائق) |
| **GOOGLE_SHEETS_SETUP.md** | خطوات إعداد Google Sheets |
| **PROJECT_SUMMARY.md** | هذا الملف |

---

## 🆘 استكشاف الأخطاء

| المشكلة | الحل |
|--------|------|
| API لا يعمل | تأكد من تشغيل Backend على port 5000 |
| لا يمكن الاتصال بـ Sheets | تأكد من مشاركة الورقة مع Service Account |
| خطأ في credentials | تأكد من وجود credentials.json في مجلد backend |
| CORS error | Backend مشغل على 5000 و Frontend على 3000 |

---

## 🎓 ملاحظات تقنية

1. **Google Sheets API**
   - الاتصال الفعلي يتم من Backend فقط
   - Frontend لا يتصل مباشرة بـ Google Sheets

2. **توكن JWT**
   - صلاحية التوكن 24 ساعة
   - يُخزن في LocalStorage

3. **كلمات المرور**
   - مشفرة بـ bcryptjs
   - لا تُخزن بصيغة واضحة

4. **الفواتير**
   - كل فاتورة لها timestamp فريد
   - ربط بتفاصيل كل مادة

---

## 📞 التطوير المستقبلي

### يمكن إضافة:
- [ ] لوحة تقارير متقدمة
- [ ] تصدير البيانات إلى PDF
- [ ] تنبيهات عند نقص المواد
- [ ] نظام الأرشيف
- [ ] تطبيق موبايل (React Native)
- [ ] نظام المرفقات (الصور/الفيديو)

---

## ✅ حالة المشروع

**المرحلة**: الإصدار الأول (v1.0)
**الحالة**: جاهز للاستخدام

---

**تم إنشاء هذا المشروع بنجاح!** 🎉

للبدء السريع، اقرأ **QUICKSTART.md**